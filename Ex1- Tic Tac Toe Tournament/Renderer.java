/**
 * This is the Renderer Interface.
 */
public interface Renderer {
    /**
     * The renderBoard method gets the board(console or none) and generates the board.
     * @param board - console or none
     */
    public void renderBoard(Board board);
}
